import { createDrawerNavigator } from "@react-navigation/drawer";

// Root Menu: Root Navigator
const RootDrawerNavigator = createDrawerNavigator(); // Returns an Object containing navigator and screen properties

export default RootDrawerNavigator; 